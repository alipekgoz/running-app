export const CLAIM_RULE_CONFIG = {
  captureConfirmationEnabled: true,
  captureCandidateThresholdPercent: 90,
  captureCooldownPlaceholderMs: 0,
  highConflictThresholdPercent: 40,
  rejectOverlapThresholdPercent: 15,
  smallOverlapAllowancePercent: 15,
} as const;
